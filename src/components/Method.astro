---
import type { Locale } from '../data/site';
import { tr, method } from '../data/i18n';

interface Props {
  locale: Locale;
}
const { locale } = Astro.props;
---

<section id={locale === 'ru' ? 'metod' : 'method'} class="scroll-mt-20 border-b border-slate-1/20 py-20 md:py-28">
  <div class="shell">
    <div class="reveal grid gap-6 md:grid-cols-[auto_1fr] md:gap-14">
      <p class="plate md:pt-3">{tr('methodPlate', locale)}</p>
      <div>
        <h2 class="text-big">{tr('methodTitle', locale)}</h2>
        <p class="mt-5 max-w-2xl text-[1.02rem] leading-relaxed text-slate-2">{tr('methodLead', locale)}</p>
      </div>
    </div>

    <ol class="mt-14 grid gap-px bg-slate-1/25 sm:grid-cols-2 lg:grid-cols-3">
      {
        method.map((m) => (
          <li class="reveal group relative bg-steel-50 p-7 transition-colors hover:bg-steel-100 md:p-8">
            <span class="font-mono text-[2.6rem] leading-none font-bold text-steel-200 transition-colors group-hover:text-brass">
              {m.n}
            </span>
            <h3 class="mt-4 text-[1.15rem] leading-tight">{m.title[locale]}</h3>
            <p class="mt-1.5 font-mono text-[0.7rem] tracking-wider text-moss uppercase">{m.dur[locale]}</p>
            <p class="mt-4 text-[0.92rem] leading-relaxed text-slate-2">{m.text[locale]}</p>
          </li>
        ))
      }
    </ol>
  </div>
</section>
